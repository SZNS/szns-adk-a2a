# Any new environment variables should be updated in both .env files for local development
# and in the Makefile "backend" command so it is properly passed to the Cloud Run instance on deployments

HAIKU_VALIDATOR_AGENT_URL=http://localhost:8001
HAIKU_UTILITIES_AGENT_URL=http://localhost:8002
